# Prisma Layer

The executable Prisma schema lives in `/prisma/schema.prisma` so Prisma CLI can discover it with the default convention.

This folder is reserved for Prisma-facing application helpers, seed mappers, and repository abstractions as the platform grows.
